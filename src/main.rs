use ilearn::{run, Config};
use num_enum::{IntoPrimitive, TryFromPrimitive};
use std::{
    fmt::{Debug, Display},
    ops::{Add, Deref, DerefMut, Index},
};

fn main() {
    /*
     * ## Deref 解引用
     */
}
